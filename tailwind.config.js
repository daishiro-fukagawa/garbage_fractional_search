module.exports = {
    purge: ['./src/**/*.vue'], // Vue.js プロジェクトの場合
    darkMode: false, // or 'media' or 'class'
    theme: {
      extend: {},
    },
    variants: {
      extend: {},
    },
    plugins: [],
  }
  